{
  "name": "rivergaming",
  "version": "1.0.0",
  "description": "about steaming",
  "main": "server.js",
  "scripts": {
    "build":"cd backend && npm i && cd .. && cd frontend && npm i && npm run build",
    "start":"node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}

{
  "name": "rivergaming",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run install-all && concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.5.0",
    "express": "^5.1.0",
    "nodemon": "^3.1.10",
    "vite": "^6.3.5"
  }
}
